# Version 1.4 — Closing Center

> **Theme:** Carry underwritten, matched deals through due diligence to a closed assignment.
> **Status:** 🟡 In progress. Depends on Opportunities pipeline (done), Buyer Matching (done), Documents (present), Underwriting (1.3). Architecture ratified — see [`CLOSING_CENTER_ARCHITECTURE_LOCK.md`](../architecture/CLOSING_CENTER_ARCHITECTURE_LOCK.md).

## Delivery status
- **Slice 1 — Closing Foundation + Due Diligence + PAID gate:** ✅ **LIVE in production** (released 2026-07-15; prod 22→**23 migrations**, serving `q0k2nXlweILTSGL6K8rS7`; merged to `main` at `8cfb343`, dual-pushed gitea+github). Delivers the domain model (versioned template → one-way snapshot → checklist → items), the `DUE_DILIGENCE` category, the `CLOSING` RBAC resource with an **ADMIN-only, reasoned, audited waiver** for required items, the server-authoritative pure `isClosingReady` PAID gate composed with `canMoveStage`, and a blocked-gate explanation that lists the outstanding required items. Behavior re-proven on the guarded `_test` DB (52 assertions); prod schema drift empty; V1.3 underwriting/offer-memo/documents/opportunity surfaces verified intact.
- **Slice 2 — Escrow:** ✅ **LIVE in production** (released 2026-07-16; prod 23→**24 migrations**, serving `hJJCViPhweeyHioi_UMkP`; merged to `main` at `53847f4`, dual-pushed). A first-class `EscrowRecord` (1:1 Opportunity) with the `NOT_OPENED→OPENED→DEPOSITED→{RELEASED|REFUNDED|FORFEITED}` lifecycle; whole-USD `earnestAmountUsd`; free-text holder; proof-of-deposit Document link; **immutable append-only `EscrowEvent`** snapshot on terminal transitions with the record frozen after (EC-I/EC-11); `CLOSING` RBAC + **ADMIN-only** reasoned resolution (EC-G); the **PAID gate is unchanged** — escrow gates PAID only via a required `ESCROW` checklist item (EC-H); optional/explicit checklist-sync (EC-J). Human workflow outside the underwriting engine (EC-1/EC-9/EC-10). Behavior re-proven on `_test` (33 assertions); prod drift empty; V1.3 + Slice 1 verified intact. Lock: [Closing Center Architecture Lock §8–10](../architecture/CLOSING_CENTER_ARCHITECTURE_LOCK.md) (EC-A…EC-J, EC-1…EC-11).
- **Slice 3 — Financing:** ✅ **LIVE in production** (released 2026-07-16; prod 24→**25 migrations**, serving `YJdWgq0rNRz7tPNraoOhD`; merged to `main` at `a29067a`, dual-pushed). A first-class `FinancingRecord` (1:1 Opportunity) tracking the lender's process — `NOT_STARTED→APPLIED→COMMITTED→CLEARED→FUNDED` with `DENIED`/`WITHDRAWN` off-ramps — as human operational workflow outside the underwriting engine. Milestone timestamps + free-text lender; commitment/appraisal Document links (scalar ids, FC-E); **no money fields** (FC-5). Terminal resolution captures an **immutable FC-J snapshot inside the record** (lender + commitment/appraisal doc + actor + reason) and freezes it — **no separate event ledger** (FC-I, verified live: 0 `financing_events` tables). `CLOSING` RBAC + **ADMIN-only** reasoned resolution (FC-G); the **PAID gate is unchanged** — financing gates PAID only via a required `FINANCING` checklist item (FC-H). The **FC-0/FC-15 reference boundary** holds: the panel shows the active scenario's underwritten debt **read-only** through the `getActiveScenarioResult` seam, never persisting/caching it, and displays "No active underwriting available." when there is none. Behavior re-proven on `_test` (44 assertions incl. FC-0: the full lifecycle writes no underwriting row); prod drift empty; V1.3 + Slices 1 & 2 verified intact. Lock: [Closing Center Architecture Lock §11–14](../architecture/CLOSING_CENTER_ARCHITECTURE_LOCK.md) (FC-0…FC-J, FC-1…FC-15).
- **UI preparation slice — Closing Center accordion:** ✅ **LIVE in production** (released 2026-07-16; serving `N4WIQvz0k7RSXD_iurA9u`; merged to `main` at `fe077f1`, dual-pushed; **code-only — no migration, prod stays at 25**). A **presentation-only** refactor (not a new domain capability): the Opportunity detail page groups Due Diligence / Escrow / Financing into one labelled **Closing Center** container — a persistent readiness header (rendering the authoritative `closingReadinessSummary`, a pure composition of `closingProgress`/`blockingItems`/`closingBlockMessage` — no second calculation) over accessible accordion sections (real `<button>` triggers, `aria-expanded`/`aria-controls`, labelled `role="region"` panels, keyboard-operable, status as text not color alone; Checklist defaults open). Each existing domain card is **unchanged** and receives the same props. **No schema/service/RBAC/lifecycle/PAID-gate change.** Verified in a real browser via a new **test-only Playwright (Chromium) harness** (`tests/visual/`): **24/24 pass** across desktop 1440×1000 / tablet 900×1100 / mobile 390×844; keyboard/a11y, role-gating, no-mutation-on-toggle, console/hydration, and long-value overflow all clean; **no defects**. Ten screenshots captured as review evidence (git-ignored under `tests/visual/.artifacts/screenshots/`: `desktop-checklist-open`, `desktop-escrow-open`, `desktop-financing-open`, `admin-terminal-controls`, `no-active-underwriting`, `nonadmin-no-terminal-controls`, `tablet-closing-summary`, `mobile-collapsed-long-blocker`, `mobile-financing-long-lender`, `mobile-escrow-long-holder`; verified 2026-07-16 with Playwright 1.61.1 / Chromium 149.0.7827.55 (`chromium-1228`) — the evidence existed at review time but is intentionally not part of repository history). Accordion commit `dd5b316` + harness/review commit `fe077f1`. See [Closing Center UI Review](./CLOSING_CENTER_UI_REVIEW.md) + [Testing Roadmap](./TESTING_ROADMAP.md).
- **Slice 4 — Assignments:** ✅ **LIVE in production** (released 2026-07-16; prod 25→**26 migrations**, serving `T6JdJGzrYR-a6lWtEhnmS`; merged to `main` at `f887adc`, dual-pushed). A first-class `AssignmentRecord` (1:1 Opportunity) capturing the wholesale transaction — the lean `NOT_STARTED→DRAFTED→{EXECUTED|CANCELLED}` lifecycle (no e-sign states), hybrid assignor/assignee parties (scalar `Seller`/`Buyer` refs and/or free-text, AS-C), the fee kept on `Opportunity.assignmentFeeUsd` as source of truth and **snapshotted only on execution** (AS-D). The assignment **agreement** reuses the v1.3 Offer-Memo **generated-document framework** (CC-F): an immutable, append-only `GENERATED` Document with a canonical snapshot + SHA-256, drafts **regenerable from current data until execution** then locked (AS-L), versioned as `Draft 1/2/3` via a **per-Opportunity `generationSequence`** anchored on a new nullable `Document.sourceOpportunityId` + its own unique key — the offer-memo (`sourceScenarioId`) path is **untouched** (AS-8/AS-E). Execution captures an **immutable snapshot inside the record** (fee, contract value, party names, agreement doc) and freezes it — no separate ledger (AS-H). `CLOSING` RBAC + **ADMIN-only** `canExecuteAssignment` (AS-G). Strictly OUTSIDE the underwriting engine (AS-I/AS-13, verified: the full lifecycle writes no underwriting row). **AS-J was revised before release** — Assignments do **NOT** seed the default checklist template; Closing policy stays configurable and consistent with Escrow/Financing, and executing an assignment never auto-completes a (configured) `ASSIGNMENT` item; the PAID gate is unchanged. Behavior re-proven on `_test` (`e2e-assignment.mjs`, 52 assertions) + extended Playwright visual coverage (**32/32 pass**); prod drift empty; offer-memo/checklist/escrow/financing/underwriting verified intact. Lock: [Closing Center Architecture Lock §15–17](../architecture/CLOSING_CENTER_ARCHITECTURE_LOCK.md) (AS-A…AS-N, AS-1…AS-15).
- **Slice 5 — Transaction Dashboard:** ✅ **LIVE in production** (released 2026-07-16; **code-only — no migration, prod stays at 26 migrations**, serving `I1QNF8TL8U1e-EuGPHtp1`; merged to `main` at `108a8bc`, dual-pushed). A **read-only, cross-opportunity, current-state PROJECTION** of every deal in-flight past `UNDER_CONTRACT` (stages `UNDER_CONTRACT`/`BUYER_MATCHED`/`CLOSING`; `PAID` opt-in) — readiness + blockers (reusing the authoritative `closingReadinessSummary`/`blockingItems`), per-domain Escrow/Financing/Assignment status, the next/overdue milestone (deterministic selection with an **injected reference date**), and the responsible party — each row linking **out** to the Opportunity's Closing Center. Pure `lib/transaction-dashboard.ts` (+ thin read-only `lib/transaction-dashboard-service.ts`) + a `CLOSING`-read-gated route `app/(workspace)/closing/page.tsx` with GET-link filters; a new "Closing" nav entry. **No new persistence, no migration, no schema change, no ActivityLog dependency, no mutating action** (TX-2/TX-3), and the Opportunity list is untouched (TD-L — roadmap #7 stays a separate follow-up). Ratified TX-0/TX-1/TX-2/TX-3/TX-A + TD-A…TD-L / **TD-1…TD-12** (incl. the pre-release refinements: TD-10 deterministic ordering, TD-11 graceful degradation, TD-12 projection immutability). Behavior proven on `_test` (`e2e-transaction-dashboard.mjs`, 21 assertions incl. no-writes/byte-identical + org isolation) + 46 Playwright visual tests; prod drift empty; V1.3 + Slices 1–4 verified intact. Lock: [Closing Center Architecture Lock §18](../architecture/CLOSING_CENTER_ARCHITECTURE_LOCK.md) + [Decision Package](../architecture/TRANSACTION_DASHBOARD_DECISION_PACKAGE.md).
- **Slice 6 — Transaction Timeline (TX-0):** ✅ **LIVE in production** (released 2026-07-16; **code-only — no migration, no schema change, no `ActivityLog` index; prod stays at 26 migrations**, serving `AkKD_n2EeTREsRoFafA-N`; merged to `main` at `e955e8a`, dual-pushed, branch deleted). A **read-only, single-opportunity, chronological PROJECTION** of one deal's recorded history (stage/escrow/financing/assignment/checklist/documents/underwriting), mounted on the Opportunity detail page **beside** the Closing Center as the historical complement to the current-state view — it **supersedes** the ad-hoc 12-item Activity card. Derived at read time from events **already** recorded in `ActivityLog` (direct `opportunityId` + `createdAt` + actor + stored label/body): **no new table, migration, event replication, or write path** (Option A, code-only). Pure `lib/transaction-timeline.ts` (event classification with an `other` fallback; deterministic newest/oldest ordering with same-instant tie-breaks; snapshot-reference resolution) + thin read-only `lib/transaction-timeline-service.ts` (one org+opportunity-scoped `ActivityLog` read + offset pagination) + a read-only panel with a newest/oldest toggle + pagination via GET params (no client JS). Ratified TX-0 with **TX-4 Projection Composition** (all Closing read surfaces consume the same pure modules; no forked derivation) and **TX-5 Projection Version** *reserved only* (not implemented). Invariants **TL-1…TL-13**: source = `ActivityLog` (TL-1); as-recorded not an authoritative ledger (TL-2); deterministic ordering (TL-3); pure/immutable (TL-4); reuse domain helpers (TL-5); read-only links-out (TL-6); org-scoped fail-closed, no new RBAC (TL-7); snapshots optional enrichment (TL-8); no migration/benchmark-gated index (TL-9); **event integrity** — every entry maps to exactly one persisted event, never synthesized (TL-10); **snapshot reference** — link OUT to the artifact, never copy (TL-11); **unknown-event forward compatibility** — an unrecognized `eventType` renders as *Other* with label/body preserved, never dropped (TL-12); **snapshot link failure** — a missing artifact suppresses only the hyperlink, never the event (TL-13). Behavior proven on `_test` (16 unit cases + `e2e-transaction-timeline.mjs` 31 assertions + 7 Playwright visual tests); prod drift empty; V1.3 + Slices 1–5 verified intact. Per Option A, real production timeline usage is to be benchmarked before any additive `ActivityLog` index. Lock: [Closing Center Architecture Lock — Slice 6](../architecture/CLOSING_CENTER_ARCHITECTURE_LOCK.md) + [Decision Package](../architecture/TRANSACTION_TIMELINE_DECISION_PACKAGE.md).
- **Slice 7 — Opportunity-List Closing Badges (Roadmap #7):** ✅ **LIVE in production** (released 2026-07-16; **code-only — no migration, no schema change, no dependency change; prod stays at 26 migrations**, serving `pgfa3y2AzXBEWuUkYQ6Fo`; merged to `main` at `edd4824`, dual-pushed, branch deleted). Compact, read-only **Closing badges** beneath each Opportunity title on the **List** view — readiness (✓ Ready / ⚠ N blockers / Closing not started) + per-domain Escrow / Financing / Assignment status — summarizing closing health at a glance and linking into the Closing Center. The **final Closing read surface**, completing the read model (Closing Center · Dashboard breadth · Timeline depth · **List badges**). Realizes **TX-6 Projection Reuse** (standing principle): a pure `projectClosingBadges()` in the shared `lib/transaction-dashboard.ts` **composes** the authoritative `closingProgress`/`blockingItems` + domain status helpers — one source, many consumers, no re-derivation. The List query gains only the **minimal** Closing selects within the existing **20/page** pagination (LB-3/LB-10); the **Kanban Board is untouched** (LB-7 — its unbounded payload makes Board badges a separate benchmarked decision). Invariants **LB-1…LB-14**: stage-aware visibility (LB-9 — early leads stay quiet; `UNDER_CONTRACT`+ without a checklist shows "Closing not started"); bounded query, no N+1/`ActivityLog`/owner lookup (LB-10); links-OUT only, no inline actions (LB-11); graceful projection (LB-12); **badge stability** — the cluster keeps a constant single-row height so rows never jump as closing progresses (LB-13); **projection completeness** — every badge incl. the readiness chip's label+tone comes from the projection, the UI knows nothing more (LB-14). Behavior proven on `_test` (12 unit cases + `e2e-opportunity-badges.mjs` 16 assertions + 9 Playwright tests); prod drift empty; Board/Dashboard/Timeline + V1.3 verified intact. Lock: [Closing Center Architecture Lock — Slice 7](../architecture/CLOSING_CENTER_ARCHITECTURE_LOCK.md) + [Decision Package](../architecture/OPPORTUNITY_LIST_BADGES_DECISION_PACKAGE.md).
- **Version 1.4 — ✅ ACCEPTED · RELEASED · FROZEN (2026-07-16).** Slices 1–7 plus the accordion UI-preparation slice are LIVE; the Closing Center is a coherent operational platform (Due Diligence → Escrow → Financing → Assignments) with a complete **read model** (Dashboard breadth · Timeline depth · List badges). Accepted by the founder and frozen at tag **`v1.4.0`** / branch **`release/1.4`** (prod 26 migrations, serving `pgfa3y2AzXBEWuUkYQ6Fo`). Acceptance record: [`V1_4_ACCEPTANCE.md`](../releases/V1_4_ACCEPTANCE.md); release note: [`V1_4_RELEASE_NOTE.md`](../releases/V1_4_RELEASE_NOTE.md); whole-platform picture: [`PLATFORM_ARCHITECTURE_MAP.md`](../architecture/PLATFORM_ARCHITECTURE_MAP.md). **No feature work lands on `release/1.4`.** Deferred / reserved (non-blocking): Board-level closing badges (LB-7) and the additive `ActivityLog` index (TX-A/TL-9/LB-8), both benchmark-gated; D15 `DealAnalysis` removal (separate reviewed cleanup). **Version 2.0 (Automation & AI) is the stable-baseline successor — not yet started.**

## Goal
Everything after "under contract" in one place: the last mile from `UNDER_CONTRACT` → `BUYER_MATCHED` → `CLOSING` → `PAID` becomes a managed, checklist-driven process instead of ad-hoc.

## Scope

### 1. Due Diligence
DD checklist per opportunity (inspection, title, financials, legal); item status + owner + due date; ties into Tasks and Documents (`DocumentType.DUE_DILIGENCE`).

### 2. Escrow
Track escrow open/status, earnest money, key dates, and contingency deadlines with reminders (Notifications).

### 3. Financing
Track the buyer's financing status and contingencies (for assignment deals, the assignee's ability to close).

### 4. Assignments
The core transaction: assignment agreement generation, assignment fee (`Opportunity.assignmentFeeUsd`), assignor/assignee parties, and execution. (Mirrors the DealFlow "Agreement Generator" priority.)

### 5. Closing Checklist
A gating checklist that must be satisfied to move an opportunity to `PAID`; blocks premature stage advance.

### 6. Transaction Management
A closing dashboard: all deals in-flight past `UNDER_CONTRACT`, their blockers, dates, and responsible parties.

### 7. Closing visibility on lists (founder refinement)
Surface closing progress **on the Opportunity list/board** — e.g. a `Closing 3 / 7` badge and a `Ready to Close` / `Not Ready` state — so closing status is visible without opening each opportunity. Reuses the pure `closingProgress` / `blockingItems` helpers already shipped in slice 1; deferred past slice 1 (a read-only projection over existing checklist state, no new model). The per-opportunity **explanation of a blocked PAID move** (which required items remain) shipped with slice 1; this item generalizes that visibility to the list.

## Architecture notes
- New child entities (DD items, escrow record, closing checklist) hang off `Opportunity`, org-scoped.
- Reuse Documents for artifacts, Tasks for work items, Notifications for date reminders — don't reinvent them.
- Stage transitions gated by checklist completion (extend the existing stage-move server action).

## Dependencies
- 1.3 Underwriting (offer/LOI artifacts feed the contract).
- Documents (contract/LOI/DD storage), Notifications (deadline reminders), Tasks (DD work).

## Definition of Done (1.4)
Global DoD **plus**: an opportunity cannot reach `PAID` without a satisfied closing checklist; every closing artifact is stored and org-scoped; key-date reminders fire.

## Out of scope
Automated buyer/seller communication and AI (2.0).
