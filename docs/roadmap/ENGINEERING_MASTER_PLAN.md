# CrowdExpanse Commercial — Engineering Master Plan (EMP)

> **Status:** Living document · **Owner:** Engineering · **Last reviewed:** 2026-07-16
> This is the constitution of the project. Every feature, release, sprint, and AI session references it.
> When reality and this document disagree, fix one of them in the same change — never leave them drifted.

---

## How to use this document

1. **Before any work**, read the relevant Module Roadmap ([Volume 4](./MODULE_ROADMAPS.md)) and the target release (Volume 3 version files).
2. **Nothing skips a step.** Every change follows the [Development Lifecycle](#development-lifecycle).
3. **Every change updates the plan.** New known issue → [Technical Debt](./TECHNICAL_DEBT.md). Status change → [Executive Dashboard](./EXECUTIVE_DASHBOARD.md). New capability → its Module Roadmap.
4. **The Definition of Done is binding.** A feature is not "done" until it meets [every DoD item](#definition-of-done).

### Document map

| Volume | Document | Contains |
|---|---|---|
| 1 | This file — [Product Vision](#volume-1--product-vision) | Purpose, mission, principles, users, business model |
| 2 | This file — [System Architecture](#volume-2--system-architecture) | Current & future architecture, security, scoping, AI boundaries |
| 3 | [RELEASE_PLAN.md](./RELEASE_PLAN.md) + `VERSION_*.md` | Release strategy, per-version scope, checklists |
| 4 | [MODULE_ROADMAPS.md](./MODULE_ROADMAPS.md) | Per-module current/completed/future/dependencies/issues/testing/AI |
| 5 | [AI_ROADMAP.md](./AI_ROADMAP.md#volume-5--intelligence-roadmap) | Intelligence ladder (market → portfolio) |
| 6 | [AI_ROADMAP.md](./AI_ROADMAP.md) | AI capabilities: purpose/inputs/outputs/rules/failure/override/testing |
| 7 | [TECHNICAL_DEBT.md](./TECHNICAL_DEBT.md) | Known issues, refactors, performance, scaling, security, infra |
| 8 | [TESTING_ROADMAP.md](./TESTING_ROADMAP.md) | Unit, integration, E2E, regression, perf, security, load, DR |
| 9 | [OPERATIONS_ROADMAP.md](./OPERATIONS_ROADMAP.md) | Release, rollback, backups, monitoring, incident, maintenance |
| 10 | [EXECUTIVE_DASHBOARD.md](./EXECUTIVE_DASHBOARD.md) | Module status/version/% complete table |
| 11 | [PERFORMANCE.md](./PERFORMANCE.md) | Latency baseline, budgets, and optimization records (PQ-3/PQ-4) |
| 12 | [COMMERCIAL_INTELLIGENCE_ARCHITECTURE.md](./COMMERCIAL_INTELLIGENCE_ARCHITECTURE.md) | Version 1.2 design authority — owner/property/market/portfolio model, provenance, identity, scoring, refresh |
| — | [FEATURE_DEPENDENCIES.md](./FEATURE_DEPENDENCIES.md) | What must exist before what |

---

## Volume 1 — Product Vision

> **Founder-input notice.** Sections marked _(confirm)_ are engineering's best inference from the codebase and prior context (assignment-fee pipeline, "seller inventory is the scarce asset"). The founder should ratify or correct them; they then become binding.

### Purpose
CrowdExpanse Commercial is the operating system for a commercial real estate (CRE) **deal-origination business**: it turns raw seller leads into closed, assigned transactions. It is the system of record for sellers, buyers, properties, and the live acquisition pipeline, and the system of work for the team that moves deals from lead to paid.

### Mission _(confirm)_
Give a CRE acquisitions team an unfair operational advantage: never lose a motivated seller, never mis-underwrite a deal, never let a matched buyer go cold — and do it with a small team.

### North Star _(confirm)_
**Closed assignments per quarter** (deals reaching `PAID`), with a health metric of **closings per seller source** — because the scarce, defensible asset is *motivated seller inventory*, not buyers or software.

### Guiding Principles
1. **Seller inventory is the moat.** Optimize the system around capturing, retaining, and converting motivated sellers.
2. **Don't overbuild.** Ship the smallest slice that delivers real operational value; expand only when usage demands it. (House rule.)
3. **Deterministic before intelligent.** Prove the workflow with hand-written rules; add intelligence only over workflows that already exist and are trusted.
4. **Every deal is org-scoped and auditable.** Multi-tenant isolation and an activity trail are non-negotiable.
5. **Tests are part of the feature.** No merge without the test that protects it.
6. **The plan is code-adjacent.** Roadmap lives in the repo and moves with the code.

### Success Metrics
- **Product:** closed assignments/quarter; pipeline conversion by stage; time-in-stage; underwriting turnaround; seller-source ROI.
- **Engineering:** CI green rate; mean time to review→merge; E2E coverage of critical paths; production incidents/quarter; p95 page latency.

### Target Users _(confirm)_
- **Acquisitions** (`ACQUISITIONS`) — sources sellers, works the pipeline.
- **Analysts** (`ANALYST`) — underwrite deals in the Analyzer.
- **Dispositions** (`DISPOSITIONS`) — match and place deals with buyers.
- **Admins** (`ADMIN`) — run the org, manage the team.
- (Future external portals: sellers, buyers, partners — see DealFlow sibling framing.)

### Business Model _(confirm)_
Assignment-fee / wholesale CRE: contract motivated sellers, underwrite, and assign the contract to a capital-partner buyer for a fee (`Opportunity.assignmentFeeUsd`). Software is the internal operating leverage; the revenue is the spread on closed deals.

### Competitive Position _(confirm)_
Off-the-shelf CRM (e.g. generic pipelines) doesn't understand CRE underwriting or assignment mechanics; CRE analytics tools (CoStar-class) don't run your deal flow. CrowdExpanse Commercial is the **vertical operating system** that unifies seller inventory, underwriting, buyer matching, and closing in one org-scoped workflow.

### Long-Term Vision
From an internal deal-flow tool → to an intelligence-augmented CRE origination platform: market/owner/property/portfolio intelligence feeding underwriting and matching, with AI assistance layered on top of trusted, deterministic workflows. Automation of marketing and communication closes the loop from lead sourcing to closing.

---

## Volume 2 — System Architecture

### Current Architecture
- **Framework:** Next.js 14.2 (App Router), React 18, TypeScript. Server Components + Server Actions; minimal client JS. Pages are `force-dynamic`.
- **Data:** PostgreSQL 16 via Prisma 5. Schema is **migration-managed** (`prisma migrate`, baseline `0_init`). Author changes on the no-shadow path — `prisma migrate diff … --script` into a new `prisma/migrations/<ts>_name/` folder, then `prisma migrate deploy` (the app DB role has no CREATEDB, so `migrate dev`'s shadow DB is unavailable). Test tooling and CI run `migrate deploy`.
- **Auth:** Cookie session (`ce_commercial_session`); `middleware.ts` redirects unauthenticated traffic to `/login`; `lib/auth.ts` provides `requireUser`/`requireRole`.
- **Authorization:** Role model (`ADMIN`, `ACQUISITIONS`, `ANALYST`, `DISPOSITIONS`) via `lib/authz.ts` (e.g. last-admin protection).
- **Multi-tenancy:** Every domain model carries `organizationId`; all queries scope by it. This is the core security invariant.
- **Storage:** Documents on local filesystem under `UPLOAD_DIR` (25 MB cap, path-traversal guard in `lib/storage.ts`); metadata in Postgres.
- **Communications:** One seam — `MessageService` (`lib/email/`) owns template selection, rendering, transport selection, and an `EmailMessage` outbox (persist-then-send). Features call only `messageService.send(...)`. `MessageKind` is a **closed, typed registry** — each kind declares a payload type + template + **retry policy** (`inline-only` | `drainable` | `manual-only`), enforced at compile time. Transports are pluggable (`console` default, `smtp` via nodemailer; API providers drop in without interface change). The outbox stores **metadata only** — never the rendered body, links, or tokens (`templateVersion` gives reproducibility instead). **Invitation delivery** is the first consumer: `inline-only` (one best-effort send, no background retry, no automatic token rotation — only the explicit admin Resend rotates); drainable kinds reconstruct data from the source of truth via a per-kind resolver. Config is fail-fast in `lib/env.ts`.
- **Domain modules:** Sellers, Buyers, Properties, Opportunities (13-stage pipeline), Deal Analyzer (`DealAnalysis`), Buyer Matching (`lib/matching.ts`), Tasks, Notes, Documents, Notifications/Activity, Team (member lifecycle), Invitations (resend/token rotation), Organization Settings (`OrganizationSettings`, `lib/org-settings.ts` — configurable invite expiry + default role; drives invitation creation), Communications (`lib/email/*`, `EmailMessage` outbox — MessageService → Template → EmailTransport; closed typed message registry with per-kind retry policy; consumed by invitation delivery), Global Search.
- **Deployment:** Node build served by pm2 (`crowdexpanse-commercial`, port 3030) behind Caddy; single VPS. See [Operations](./OPERATIONS_ROADMAP.md).
- **Observability/Performance:** unauthenticated `/api/health` liveness probe (`{status, dbMs, uptime, commit}`), a zero-dep dev-gated timing helper (`lib/telemetry.ts`), a seeded measurement harness (`npm run perf:measure`), and a standardized `EXPLAIN` helper (`npm run perf:explain`) with a recorded [performance baseline](./PERFORMANCE.md) (PQ-3, observational). Optimization is evidence-driven against that baseline: **PQ-4 complete** — PQ-4a narrowed the Opportunities-board query to a dedicated `select` (board p95 ~109 → ~43 ms, ≈57%, EXPLAIN-verified, no schema/index/cache change), and with every path then well within budget no further optimization was warranted. Pagination/virtualization/indexes/caching are documented as future scalability options, revisited only if a future baseline breaches a budget.
- **Testing/CI:** 15 E2E scripts (`scripts/e2e-*.mjs`) → dedicated `_test` DB with a no-override guard, plus a **`node:test`+`tsx` unit layer** (`tests/unit/**`, branch-coverage gate ≥90% critical / ≥80% overall). GitHub Actions CI (mirror, ephemeral Postgres) runs distinct blocking gates — **Typecheck → Lint → Unit → E2E → Build** (lint is `next lint`, blocking, PQ-2). Local: `test:ci` (typecheck + unit + E2E) + explicit `npm run lint`. See [Testing](./TESTING_ROADMAP.md).

### Data Flow
Browser → middleware (session gate) → Server Component (reads via Prisma, org-scoped) / Server Action (writes, revalidate) → Postgres. Documents stream through a Server Action to `UPLOAD_DIR`. Activity/notification rows are written alongside domain writes.

### Module Relationships
`Organization` owns everything. `Seller` → `Property` → `Opportunity` (pipeline) → `DealAnalysis` (underwriting) and `BuyerMatch` (→ `Buyer`). `Task`/`Note`/`Document`/`ActivityLog` attach to opportunities and records. See [FEATURE_DEPENDENCIES.md](./FEATURE_DEPENDENCIES.md).

**Version 1.2 (Commercial Intelligence)** adds a new canonical **`Owner`** domain — the durable title-holder that bears a portfolio and accumulates enrichment — distinct from `Seller` (the transaction counterparty), plus a `Market` reference entity and a shared **provenance spine**. All additive and org-scoped; **no core workflow changes**. **Slice 1's identity foundation is complete** (1a + 1a-2): `Owner`/`OwnerAlias`/`OwnerExternalIdentifier`, nullable `Seller.ownerId`/`Property.ownerId`, a deterministic proposal-only identity library (`lib/intelligence/owner-identity.ts`), and **reversible, structural-only, ADMIN-only merge/unmerge** (`OwnerMergeRecord`, chain resolution) — all upholding the [six identity invariants](./COMMERCIAL_INTELLIGENCE_ARCHITECTURE.md#the-six-identity-invariants-non-negotiable--stable-across-all-providers); merged owners are never physically deleted. **Commit 1c is complete — the headless intelligence foundation is fully in place.** The `Observation → Signal → Projection` core pipeline (1b-1 + 1b-2) is an append-only, immutable ledger (`lib/intelligence/provenance.ts` — version-stamped, superseded-not-mutated, complete lineage) feeding a deterministic **projection engine** (`lib/intelligence/projection.ts` + `projection-precedence.ts`) that makes `Owner` columns disposable, ledger-backed projections under a total-order precedence rule (pin → asOf → confidence → source-category → id), with transactional writes, sticky overrides, and a byte-for-byte **reconstruction invariant**. Projection is *pure* — it only selects/orders/normalizes accepted Signals. On top sits the **ingestion path** (1c): a pure `SourceAdapter` contract (`lib/intelligence/sources/` — `fetch` + `map` only) and the `runRefresh` orchestrator (`lib/intelligence/refresh.ts`) that records observations → accepts signals → triggers projection, anchored by a durable `RefreshJob` (sole audit surface + idempotency). Refresh is **observational, replayable, atomic**; adapters are **pure** and stamp `adapterVersion` on every observation — so every future source (CSV, county, licensed, AI) is just another adapter while the orchestrator, ledger, and projection engine stay fixed. **Commit 1d-1 exposes this foundation through the first UI** (`app/(workspace)/owners/*`): Owner nav/list/detail/create/edit, built as a thin *consumer* — every mutation flows action → `lib/owners`/`lib/intelligence` → ledger → projection (the UI never writes `displayName`/`entityType`/`matchKey` directly), Owner detail renders each field as Projected Value → Winning Signal → Signal History, and the `OWNER` permission policy is enforced at its first call-sites. **Commit 1d-2a adds operational-graph linking** (Seller↔Owner, Property↔Owner) from both the Owner and Seller/Property pages: link, **atomic move** (re-link A→B in one `ownerId` update, audited `owner.linked`/`owner.moved`/`owner.unlinked`), and unlink — all kept strictly separate from canonical identity (linking edits only the FK and writes no Observation/Signal). **Commit 1d-2b adds standalone candidate review** — a duplicate-owner queue (`lib/intelligence/owner-duplicates` exact-matchKey + alias overlap → `lib/owner-match` decision store `OwnerMatchDecision`) where a human confirms/dismisses pairs (ADMIN reopen); dismissed pairs re-surface only on a material identity-fingerprint change. It is decision-support: it **records decisions only and never merges** — `CONFIRMED` pairs feed the 1d-3 merge queue. **Commit 1d-3a adds manual refresh controls** to Owner detail — a source-attributed trigger (`runRefresh` via the manual adapter, distinct from direct Edit) plus inline history of the 10 most recent Owner-specific `RefreshJob`s (`REFRESH` write = ADMIN/ACQUISITIONS, read = all). **Commit 1d-3b completes Slice 1 with the ADMIN-only merge/unmerge workspace** (`/owners/merges`) over the existing reversible engine, consuming `CONFIRMED` decisions: **merge is the only workflow permitted to perform structural identity change**, and **merge+decision-resolution (and unmerge+unresolution) each commit or roll back as one atomic transaction** — the merge engines were parameterized into tx-body form (`mergeOwnersTx`/`unmergeOwnersTx`, logic unchanged) so `lib/owner-merge` runs the structural change and the `OwnerMatchDecision` resolution write together (`resolvedAt`/`resolvedByUserId`/unique `mergeRecordId`; status stays `CONFIRMED`, no `MERGED` status). The winner is **suggested deterministically** (more linked records → older → smaller id, provider-neutral) but **never auto-applied** — an ADMIN explicitly confirms/swaps; unmerge returns the still-`CONFIRMED` pair to Awaiting Merge; the `OwnerMergeRecord` (+ resolution stamp) is the authoritative audit. **Slice 1 is now complete, accepted, and live in production** (1a/1a-2 → 1b → 1c → 1d-1…1d-3b). The entire 1.2 Owner UI (1d-1…1d-3b) is merged, built, prod-DB-current (10 migrations), and **deployed — serving build `9555QJiLxh4O9PrlVp3UH`, verified locally and externally**; [Tech Debt D5](./TECHNICAL_DEBT.md) (the stale-frontend blocker) is resolved with a permanent `prebuild` recurrence guard. Formal sign-off: [Slice 1 Production Acceptance Record](../releases/V1_2_SLICE_1_ACCEPTANCE.md). **Slice 2 has begun — Commit 2a extends the *same* spine to `Property`**: a dispatch-only **entity-projector registry** (`lib/intelligence/entity-registry.ts`) lets the shared `Observation → Signal → Projection` substrate serve a second canonical entity without a second pipeline; `IntelligenceEntityType` gains `PROPERTY` (prod **11 migrations**), `yearBuilt`/`squareFeet` become ledger-backed projections written through the `lib/properties.ts` domain service (operational CRUD is now a thin caller), and the **Projection Reconstruction Standard** binds every ledger-backed entity. Commit 2a landed **headless** — a data-layer landing verified in production (Owner ledger byte-for-byte unchanged). **Commit 2b then exposed Property through the UI and redeployed the app** (`app/(workspace)/properties/[id]`): per-field provenance for `yearBuilt`/`squareFeet` (Projected Value → Winning Signal → Signal History) and a `REFRESH`-gated manual-refresh surface with inline job history — a thin consumer reusing the Owner detail pattern wholesale, with the previously Owner-specific provenance component generalized to the shared `FieldProvenanceCard` now that a second real consumer exists. The 2b redeploy made the 2a ledger write-path live (build-ID flip verified on disk + externally), **closing [D13](./TECHNICAL_DEBT.md)**; it is migration-free (still 11 migrations). **Commit 2c-i then began Property *identity*** — a fresh architecture lock ([Property Identity Lock](../architecture/PROPERTY_IDENTITY_LOCK.md)), because 2c introduces a new mechanism (canonical identity) rather than extending the proven projection/refresh path. 2c-i lands headless (prod **12 migrations**, [D14](./TECHNICAL_DEBT.md)): identity anchors (`apn/FIPS/address`) ride the ledger as typed projected fields; a **derived, rebuildable `PropertyIdentity` index** carries a deterministic `identityVersion` fingerprint (`IDENTITY_ALGORITHM_VERSION` + winning anchors + normalizer versions) and a deterministic projection watermark — *canonical identity is a pure function of the accepted ledger, sitting on top of it, not beside it*; the `PropertyExternalIdentifier` crosswalk is append-only with Signal-style supersession. **Commit 2c-ii then added the Resolution layer** (prod **13 migrations**, still headless under [D14](./TECHNICAL_DEBT.md)): a **pure deterministic classifier** (`lib/intelligence/property-resolution.ts` — identical evidence + lookup ⇒ identical outcome, no DB/clock/randomness/side-effects) drives **guarded resolve-before-create** (`lib/intelligence/property-resolver.ts`) along a fixed sequence — Normalize → Lookup → Conflict Inspection → Classification → Decision → Attachment → Audit → Candidate → Rebuild. Authority hierarchy **parcel ↓ external-id ↓ address**: a unique conflict-free authoritative match resolves to the existing property (enriching it with appended, source-preserving observations, attaching the crosswalk, and recording an append-only `PropertyResolution` audit event with a deterministic `basis`); conflicts/ambiguity/address agreement create a new property + `PropertyMatchDecision` review candidates (mirrors `owner-match`, resurfacing via the 2c-i `identityVersion`). **Resolution never modifies evidence** (append + deterministic rebuild only); **replay is deterministic** (RES-6) and **reversal is a first-class append-only event that never rewrites classification history** (RES-7, `revokedByResolutionId`). Confirm/dismiss record a decision only (structural Property merge stays deferred). **Commit 2c-iii then shipped the review & resolution UI** and **redeployed** (build `4A-bszK-FtpZr-w48yTP_` → `8vRFYwF-JHfHalfXSAoSy`), making the whole 2c stack live and **closing [D14](./TECHNICAL_DEBT.md)**: `/properties/candidates` (candidate review — confirm/dismiss/reopen) and `/properties/[id]/identity` (resolution/identity detail — evidence, anchor provenance, deterministic basis, external-identifier crosswalk, append-only resolution audit, competing candidates, and reversal), built as thin consumers of the engine (read + thin action wrappers; no Evidence/Identity/Resolution change) and governed by `PROPERTY_IDENTITY`. The UI upholds the [Human Review Principles](./../architecture/HUMAN_REVIEW_PRINCIPLES.md) — it is never a second decision engine, never derives facts the engine hasn't, and keeps every identity conclusion traceable to its evidence. **Slice 2 (Property Intelligence) is now complete and live**; external sources, geocoding, structural merge, and fuzzy/AI/scoring remain deferred and separately gated. The full model, provenance, identity strategy, scoring, and refresh design are the design authority in **[Volume 12 — Commercial Intelligence Architecture](./COMMERCIAL_INTELLIGENCE_ARCHITECTURE.md)**.

**Version 1.3 (Commercial Underwriting)** begins by formalizing *ownership* around the existing pure calculation kernel rather than deepening the math — a read-only consumer of Version 1.2, not a second source of truth (design authority: the **[Underwriting Architecture Lock](../architecture/UNDERWRITING_ARCHITECTURE_LOCK.md)**, decisions U-A…U-L, invariants UW-1…UW-9). **Commit 3a (Underwriting Model Formalization) is live**: the canonical `Underwriting → Scenario → Assumption → ScenarioResult` model, anchored on the Opportunity, with model lineage (`UNDERWRITING_MODEL_VERSION`/`CALCULATION_LIBRARY_VERSION`/`RULESET_VERSION`) frozen per scenario and a deterministic `scenarioVersion` fingerprint of (assumptions + canonical ordering + lineage) — making `Scenario → ScenarioVersion → ScenarioResult` explicit. Property context enters **once** as SEEDED assumptions (a one-way *ScenarioSeed* snapshot; a Scenario never changes because the Property changes) alongside the analyst's MANUAL assumptions; `Decimal` in persistence is converted to `number` only at the kernel boundary. `ScenarioResult` is a derived, cached, **content-idempotent, rebuildable** surface (1:1 with a Scenario — *every deterministic output belongs to exactly one Scenario*) reconstructed purely from frozen assumptions + lineage + the **unchanged** `lib/analysis.ts` kernel, never from current Property state (UW-8/UW-9). The analyzer was repointed behavior-preservingly onto this model under a new `UNDERWRITING` RBAC resource; the legacy `DealAnalysis` is retained deprecated with an idempotent backfill ([D15](./TECHNICAL_DEBT.md)). **Commit 3b then deepens the math only** — a sequence of deterministic sub-slices, each a **pure sibling** to the unchanged `lib/analysis.ts` kernel and held to the [Calculation Principles](../architecture/UNDERWRITING_CALCULATION_PRINCIPLES.md), released one per production release. **3b-i (deterministic debt sizing) is live**: pure `lib/underwriting/debt-sizing.ts` sizes the loan by the binding of LTV/LTC/DSCR ceilings computed independently (`sizedLoan = min(` applicable constraints `)`), lineage bumped to v2, invariants DS-1…DS-4. **3b-ii (income & expense schedules) is live**: pure `lib/underwriting/schedule.ts` rolls optional per-scenario `ScenarioLineItem`s up **independently** to the effective gross income and operating expenses fed to the kernel — a present schedule overrides the scalar assumption per kind, an absent/cleared one falls back to the scalar (3a behavior preserved); the fingerprint includes canonically-sorted line items but excludes presentation-only `position`, lineage bumped to v3, invariants IS-1…IS-3. **3b-iii (financing cases + multi-year cash flow) is live**: first-class **FinancingCase** capital structures beneath a Scenario (`Scenario` operating → `FinancingCase` capital → `CashFlow` per-case), pure `lib/underwriting/cash-flow.ts` (a financing-independent NOI trajectory reused by every case) + `financing.ts` (each case layers its debt via the unchanged kernel + debt-sizing into levered cash flow before tax). To honor the ownership split, capital was **relocated off the Scenario** — the 6 capital assumptions moved to `FinancingAssumption` and the 8 financing-dependent columns dropped from `ScenarioResult` onto `FinancingCaseResult` (a sanctioned one-time reshape made while prod held zero underwriting rows) — so `ScenarioResult` is operating-only; lineage bumped to v4, invariants CF-1…CF-5, operating cash flow only. **3b-iv (exit valuation + basic equity waterfall) is live**: the next projection layer per case (`… → CashFlowYear → Exit Valuation → Equity Cash Flows → Return Metrics`), pure `lib/underwriting/exit.ts` — terminal value (exit-year NOI ÷ exit cap rate), selling costs, the **true amortized** remaining loan balance (never a shortcut), net sale proceeds, the equity cash-flow series (final year = operating CF + sale, counted once), and returns (equity multiple, levered IRR via deterministic bisection, total profit). Exit assumptions are operating (financing-independent) and feed each case's fingerprint via `scenarioVersion`; a basic single-holder waterfall only; lineage bumped to v5, invariants EX-1…EX-6. **3b-v (sensitivity matrices) is live**: a per-`FinancingCase` sensitivity layer that is a pure **consumer** of the engine (Principles 10–11), not another calculator — `SensitivityAnalysis` (one immutable baseline case) → `SensitivityCell` grid. Pure `lib/underwriting/sensitivity.ts` generates deterministic evenly-spaced axis values (≤11/axis, ≤121 cells) and re-derives a chosen target metric (levered IRR / equity multiple / total profit / cap rate / DSCR) over each cell's overrides — applied to an **in-memory copy** of the frozen assumptions, **never persisted** — storing only the reading; fixed axis + metric allow-lists, `sensitivityVersion = f(financingCaseVersion, canonical spec, lineage)`, lineage bumped to v6, the baseline cell marked only on **exact** axis match (never snapped), invariants SE-1…SE-7. Additive migration (prod **19 migrations**). **3b-vi (findings/risks + suggested recommendation) is live**: the TOP consumer layer — pure `lib/underwriting/findings.ts` runs a fixed, versioned ruleset over the settled deterministic outputs and emits per-`Scenario` findings + one advisory recommendation (`PROCEED` / `PROCEED_WITH_CONDITIONS` / `PASS`); it never feeds back into a calculation (Principle 7 / UW-7) and the engine never decides (UW-4). The **ratified R-A fingerprint separation** removes `RULESET_VERSION` from every metric fingerprint and moves it into a dedicated `findingsVersion`, so a rules-only change (ruleset **1→2**) re-fingerprints only the findings and never invalidates a metric (FR-6); model + calc lineage unchanged. Invariants FR-1…FR-6. Additive migration (prod **20 migrations**). **Commit 3d (decided recommendation + `UNDERWRITING_APPROVAL`) is live**: the U-B human half — a **non-deterministic, authored, terminal** `UnderwritingDecision` (`APPROVED`/`DECLINED`/`DEFERRED`) recorded only on a LOCKED scenario against an immutable snapshot (`scenarioVersion` + `findingsVersion`). It lives entirely OUTSIDE the pure engine (UW-4) and never feeds back into it (Principle 7 / UW-7 / AP-3); it is an operational record — **no lineage, no fingerprint, no version, no rebuild** — so `lib/analysis.ts` and every pure calc module stay untouched and no lineage bumps. Append-only + immutable (AP-4); governed by a NEW `UNDERWRITING_APPROVAL` RBAC resource separate from authoring (separation of duties, AP-5). Invariants AP-1…AP-6. Additive migration (prod **21 migrations**). **Commit 3e (scenario comparison) is live**: a **read-only** side-by-side of every Scenario version for an Opportunity (`/analyzer/[opportunityId]/compare`) — a pure consumer honoring **Calculation Principle 5** (comparing scenarios is a read over independently-computed, separately-persisted results, never a computation that entangles them). Each version's operating metrics + primary `FinancingCase` returns + suggested recommendation + current decision render as columns × metric rows, ordered by version, each read from that Scenario's own persisted results — so editing/creating a later version never alters a prior version's metrics. `UNDERWRITING`-read-gated; **no new model, migration, fingerprint, lineage bump, or RBAC** (model 6 / calc 6 / rules 2 unchanged; `lib/analysis.ts` untouched) — a code-only release (prod stays **21 migrations**). The final 1.3 DoD item — **offer-memo generation** — is **LIVE** (prod 21→**22**, serving `OuE0HfLIzVy6LsKqzp3ct`; design authority: the **[Offer-Memo Architecture Lock](../architecture/OFFER_MEMO_ARCHITECTURE_LOCK.md)**, OM-1…OM-12). It is a **Documents-owned generated artifact**: a LOCKED scenario's settled outputs are read through a single narrow one-way seam (`getScenarioForMemo`, fail-closed), assembled into an immutable canonical snapshot, and rendered by a pure deterministic self-contained-HTML template into an append-only, SHA-256-stamped `Document` (origin GENERATED) that never mutates when the model later changes (OM-3/OM-4/OM-6/OM-7/OM-8). Generation crosses two domains and requires both `UNDERWRITING` read and `DOCUMENT` write (never `UNDERWRITING_APPROVAL`, OM-K); the `Document` model was extended additively (prod 21→**22** at release). `lib/analysis.ts` and every pure underwriting module are untouched (OM-10/OM-11). **With offer-memo generation shipped, Version 1.3 (Commercial Underwriting) is feature-complete** — every Definition-of-Done item is met. LOI generation, PDF output, and market-signal risks remain deferred to later sibling slices, separately gated.

**Version 1.4 (Closing Center)** carries an underwritten, matched deal the last mile — `UNDER_CONTRACT → BUYER_MATCHED → CLOSING → PAID` becomes a managed, checklist-gated process — and is the deliberate **opposite** of the underwriting engine: human, operational workflow that reuses the existing platforms (Documents, Tasks, ActivityLog, the pipeline) rather than reinventing them (design authority: the **[Closing Center Architecture Lock](../architecture/CLOSING_CENTER_ARCHITECTURE_LOCK.md)**, decisions CC-A…CC-G, invariants CC-1…CC-10). **Slice 1 (Closing Foundation + Due Diligence + the PAID gate) is live** (prod 22→**23**, serving `q0k2nXlweILTSGL6K8rS7`). A first-class **`ClosingChecklist`** (1:1 with an `Opportunity`) of typed `ClosingChecklistItem`s is instantiated by a **one-way SNAPSHOT** of a versioned `ClosingChecklistTemplate` — the same snapshot discipline as the 1.3 ScenarioSeed, so an instantiated checklist is immutable with respect to later template evolution (CC-G/CC-9/CC-10); a seeded default Due-Diligence template is *data*, not hardcoded logic, and each item declares a `completionEvidenceType` (NONE/DOCUMENT/TASK/MANUAL). The **PAID gate** is a **pure** predicate — `isClosingReady` (every required item COMPLETE or WAIVED) — COMPOSED with, never replacing, the existing role-based `canMoveStage` (CC-2/CC-3); enforcement is server-side, and a blocked move now **explains itself**, listing the outstanding required items via the same pure `blockingItems`/`closingBlockMessage` the UI renders (so the message and the gate can never disagree). A new **`CLOSING`** RBAC resource governs the checklist (write ADMIN/ACQUISITIONS/DISPOSITIONS, read +ANALYST), and **waiving a required item is a stricter ADMIN-only, reasoned, audited override** (CC-5); every state change is recorded in ActivityLog (CC-6). It never reads, writes, or participates in the deterministic underwriting engine — `lib/analysis.ts`, the 1.3 locks, lineage, and fingerprints are untouched (CC-1). The additive migration adds 3 enums + 4 tables (0 destructive), so the frozen `v1.3.0` baseline is unaffected. Assignment-agreement generation (which will inherit the Offer-Memo generated-document framework, CC-F), the transaction dashboard, date-triggered reminders, and list-level closing-progress badges (roadmap #7) remain deferred to later slices, each separately gated. **Slice 2 (Escrow) is live** (prod 23→**24**, serving `hJJCViPhweeyHioi_UMkP`; lock extended, decisions EC-A…EC-J, invariants EC-1…EC-11). Escrow is modeled as a **first-class financial process, not a checklist item** — a `EscrowRecord` (1:1 Opportunity) carrying the `NOT_OPENED→OPENED→DEPOSITED→{RELEASED|REFUNDED|FORFEITED}` lifecycle, whole-USD earnest money (EC-C; `Decimal` still reserved for underwriting), a free-text holder (no `Party` model, EC-D), and a proof-of-deposit Document link (EC-F). The append-only philosophy of the 1.3 `UnderwritingDecision` is reused: a terminal transition writes an **immutable `EscrowEvent` snapshot** (amount/holder/proof/actor at that instant) and the record is thereafter **frozen** (EC-I/EC-11), so a later edit can never rewrite a historical financial event. Terminal resolution is **ADMIN-only** and reasoned (`canResolveEscrow`, EC-G/EC-4); ordinary escrow work reuses `CLOSING`. Critically, **the PAID gate is unchanged** — escrow is *not* hardcoded into it; an org makes escrow blocking by adding a required `ESCROW` checklist item, keeping policy in configuration (EC-H), and an optional/explicit/never-automatic affordance lets a human mark that item complete once deposited (EC-J). Escrow never touches `lib/analysis.ts` or any underwriting/offer-memo surface (EC-1/EC-9/EC-10); the additive migration (2 enums + 2 tables) leaves the frozen 1.3 baseline and Slice 1 untouched. **Slice 3 (Financing) is live** (prod 24→**25**, serving `YJdWgq0rNRz7tPNraoOhD`; lock extended, decisions FC-A…FC-J, invariants FC-0…FC-15). Financing is a **first-class `FinancingRecord`** (1:1 Opportunity) tracking the lender's process — `NOT_STARTED→APPLIED→COMMITTED→CLEARED→FUNDED` with `DENIED`/`WITHDRAWN` off-ramps, a pure unit-tested transition guard (FC-10), milestone timestamps, a free-text lender (no `Party` model), and commitment/appraisal Document links (scalar ids, FC-E). Deliberately **lighter than Escrow**: it carries **no money fields** (FC-5; `Decimal`/operational USD both absent — financing economics live only in the 1.3 underwriting engine), and rather than a separate event ledger it captures the terminal outcome as an **immutable FC-J snapshot inside the record** (lender + commitment/appraisal doc + actor + reason) and then **freezes** the record (FC-6/FC-I) — verified live: 0 `financing_events` tables. Terminal resolution is **ADMIN-only** and reasoned (`canResolveFinancing`, FC-G); ordinary work reuses `CLOSING`; every change is audited (FC-7). The **PAID gate is unchanged** — financing gates PAID only via a required `FINANCING` checklist item (FC-H), never hardcoded. The load-bearing **FC-0 reference boundary** (reinforced by **FC-15**) governs its one touchpoint with underwriting: the Financing panel reads the active scenario's sized debt **read-only** through the existing `getActiveScenarioResult` seam at render time and **never owns, copies, caches, or persists** loan amount / DSCR / debt yield / binding constraint — the reference is an ephemeral view that shows "No active underwriting available." when absent, and the full lifecycle provably writes **no** underwriting row (FC-1/FC-11…FC-15, E2E-verified). The additive migration (1 enum + 1 table) leaves the frozen 1.3 baseline and Slices 1 & 2 untouched. A subsequent **UI-preparation slice (Closing Center accordion)** is live (code-only, serving `N4WIQvz0k7RSXD_iurA9u`, `main` @ `fe077f1`, **prod stays at 25 migrations**): a presentation-only refactor grouping Due Diligence / Escrow / Financing into one labelled Closing Center container — a persistent readiness header (the authoritative `closingReadinessSummary`, a pure composition of the existing gate helpers) over accessible accordion sections — before Assignments is added, so the fourth domain lands into structure rather than a flat stack. Each existing card is unchanged; **no schema/service/RBAC/lifecycle/PAID-gate change**. It also introduced a **test-only Playwright (Chromium) visual-verification harness** (`tests/visual/`, a devDependency absent from the production bundle) that runs authenticated cross-viewport UI checks against the `_test` DB — 24/24 passing, no defects — reusable for future Closing Center UI work. **Slice 4 (Assignments) is live** (prod 25→**26**, serving `T6JdJGzrYR-a6lWtEhnmS`, `main` @ `f887adc`; lock extended §15–17, decisions AS-A…AS-N, invariants AS-1…AS-15) — the core wholesale transaction as a first-class **`AssignmentRecord`** (1:1 Opportunity) with the lean `NOT_STARTED→DRAFTED→{EXECUTED|CANCELLED}` lifecycle (no e-sign states), hybrid assignor/assignee parties (scalar `Seller`/`Buyer` refs and/or free-text, AS-C), and the fee kept on `Opportunity.assignmentFeeUsd` as the single source of truth, **snapshotted only on execution** (AS-D). This is the first Closing slice that **bridges two frameworks**: the operational Closing record and the v1.3 **generated-document infrastructure** — the assignment agreement inherits the Offer-Memo framework (CC-F) as an immutable, append-only `GENERATED` Document (canonical snapshot + SHA-256), **regenerable from current data until execution then locked** (AS-L) and versioned `Draft 1/2/3` via a **per-Opportunity `generationSequence`** anchored on a new nullable `Document.sourceOpportunityId` + its own unique key — the offer-memo `sourceScenarioId` path is **untouched** (AS-8/AS-E). Execution captures an **immutable snapshot inside the record** (fee, contract value, party names, agreement doc) and freezes it — no separate ledger (AS-H, mirroring FC-J). `CLOSING` RBAC + **ADMIN-only** `canExecuteAssignment` (AS-G); the agreement reads **only operational data**, never the underwriting engine (AS-I/AS-13/AS-14; `lib/analysis.ts` untouched). **AS-J was revised before release**: Assignments do **not** seed the default checklist template — Closing policy stays configurable and consistent with Escrow/Financing, executing never auto-completes a (configured) `ASSIGNMENT` item, and the PAID gate is unchanged. The additive migration (1 enum + 1 table + 1 nullable Document column + 1 unique key) leaves the frozen 1.3 baseline and Slices 1–3 untouched. **Slice 5 (Transaction Dashboard) is live** (**code-only — no migration, prod stays at 26**, serving `I1QNF8TL8U1e-EuGPHtp1`, `main` @ `108a8bc`; lock §18, decisions TD-A…TD-L, invariants TD-1…TD-12) — a **read-only, cross-opportunity, current-state PROJECTION** of every deal in-flight past `UNDER_CONTRACT`, deliberately the **opposite of a new operational domain**: it derives every value at read time from the existing Closing/Escrow/Financing/Assignment records (TX-2 projection purity — nothing cached, persisted, or materialized) and contains **no mutating action** — every row links out to the Opportunity's Closing Center (TX-3 orchestration, not ownership). Pure `lib/transaction-dashboard.ts` (inclusion predicate; deterministic next-milestone selection with an injected reference date; a presentation-neutral per-row view-model reusing the authoritative `closingReadinessSummary` + status helpers so the dashboard can never disagree with the Closing Center) + a thin read-only service + a `CLOSING`-read-gated `/closing` route with GET-link filters. It adds **no persistence, no migration, no schema change, and no `ActivityLog` index** (TX-A — benchmark first; the in-flight query rides the existing `organizationId` index + the `opportunityId @unique` 1:1 joins). The founder pre-release refinements are folded in: **TD-10** deterministic DB-order-independent row ordering (overdue → soonest milestone → stage → title → id), **TD-11** graceful degradation (a missing domain record never suppresses a row), and **TD-12** projection immutability (plain data in, new array out — a reusable contract for roadmap #7, which stays a SEPARATE follow-up: the Opportunity list is untouched, TD-L). The Transaction Timeline (TX-0, the single-opportunity chronological read-model) remains reserved and is the intended next milestone. **Slice 6 (Transaction Timeline, TX-0) is live** (**code-only — no migration, no schema change, no `ActivityLog` index, prod stays at 26**, serving `AkKD_n2EeTREsRoFafA-N`, `main` @ `e955e8a`; lock Slice 6, decisions TX-0/TX-4/TX-5, invariants TL-1…TL-13) — a **read-only, single-opportunity chronological PROJECTION** of one deal's recorded history mounted on the Opportunity detail page **beside** the Closing Center (the historical complement to the Dashboard's breadth), **superseding** the ad-hoc Activity card. It is derived entirely at read time from events **already** in `ActivityLog` (direct `opportunityId`/`createdAt`/actor/label/body) — **no new table, migration, event replication, or write path** (Option A). Pure `lib/transaction-timeline.ts` (classification with an `other` fallback; deterministic newest/oldest ordering; snapshot-reference resolution) + a thin read-only service (one org+opportunity-scoped read + offset pagination) + a read-only panel (newest/oldest toggle + pagination via GET params, no client JS). It establishes **TX-4 Projection Composition** as a standing principle — Dashboard, Timeline, and the future list badges (#7) all consume the *same* pure modules, never a forked derivation — and **reserves** (does not implement) **TX-5 Projection Version**. TL-10 event integrity (every entry is a persisted event, never synthesized), TL-11 snapshot reference (link out, never copy), TL-12 unknown-event forward compatibility (render as *Other*, never drop), and TL-13 snapshot link failure (suppress only the link, never the event) keep it an honest historical view. Proven on `_test` (16 unit + 31 E2E + 7 Playwright); prod drift empty; V1.3 + Slices 1–5 intact; real production timeline usage will be benchmarked before any additive `ActivityLog` index. **Slice 7 (Opportunity-list Closing Badges, Roadmap #7) is live** (**code-only — no migration, no schema/dependency change, prod stays at 26**, serving `pgfa3y2AzXBEWuUkYQ6Fo`, `main` @ `edd4824`; lock Slice 7, TX-6, LB-1…LB-14) — the **final Closing read surface**: compact read-only closing-health badges (readiness + Escrow/Financing/Assignment) beneath each Opportunity title on the **List** view, linking into the Closing Center. It realizes **TX-6 Projection Reuse** — a pure `projectClosingBadges()` in the shared `lib/transaction-dashboard.ts` composes the authoritative `closingProgress`/`blockingItems` + domain helpers (one source, many consumers; no re-derivation) — adding only the minimal Closing selects to the existing 20/page List query (no N+1, no `ActivityLog`, no owner lookup), leaving the unbounded Kanban **Board untouched** (LB-7). Stage-aware visibility keeps early leads quiet and shows "Closing not started" for `UNDER_CONTRACT`+ without a checklist (LB-9); LB-13 keeps the cluster a constant height (rows never jump); LB-14 has the projection produce every badge (incl. the readiness chip's label+tone) so the UI knows nothing more. Proven on `_test` (12 unit + 16 E2E + 9 Playwright); prod drift empty; Board/Dashboard/Timeline + V1.3 intact. **Version 1.4 is ✅ ACCEPTED · RELEASED · FROZEN (2026-07-16)** — Slices 1–7 plus the accordion UI-preparation slice are LIVE (Due Diligence → Escrow → Financing → Assignments + a complete read model: Dashboard breadth · Timeline depth · List badges). Accepted by the founder and frozen at tag **`v1.4.0`** / branch **`release/1.4`** (prod 26 migrations, serving `pgfa3y2AzXBEWuUkYQ6Fo`); acceptance record [`V1_4_ACCEPTANCE.md`](../releases/V1_4_ACCEPTANCE.md), release note [`V1_4_RELEASE_NOTE.md`](../releases/V1_4_RELEASE_NOTE.md), whole-platform picture [`PLATFORM_ARCHITECTURE_MAP.md`](../architecture/PLATFORM_ARCHITECTURE_MAP.md). No feature work lands on `release/1.4`. **Version 1.4 is the stable baseline for Version 2.0 (Automation & AI), which is NOT yet started.** Deferred/reserved (non-blocking): Board-level badges (LB-7), the additive `ActivityLog` index (TX-A/TL-9/LB-8), and the D15 `DealAnalysis` removal (separate reviewed cleanup).

**Version 2.0 (Automation & AI)** — architecture **FOUNDER RATIFIED 2026-07-16** (design only; each phase separately approved before implementation). Automation is a first-class **bounded orchestration domain** that sits *beside* the spine: it **reads** projections/events, **evaluates policy**, **calls existing domain services through the same human seams a person uses**, and **records its own execution** in an immutable operational ledger — it never owns authoritative business state, never becomes an underwriting calc input, never bypasses UNDERWRITING_APPROVAL / Closing readiness / the composed PAID gate, never mutates snapshots or overwrites `ActivityLog`, never crosses orgs, and never treats AI output as fact (design authority: the **[Automation Architecture Lock](../architecture/AUTOMATION_ARCHITECTURE_LOCK.md)**, refinements A1–A8, invariants AU-1…AU-13; the **[Decision Package](../architecture/VERSION_2_0_DECISION_PACKAGE.md)** and **[Discovery Report](../architecture/VERSION_2_0_DISCOVERY.md)**; and **[Platform Map §6](../architecture/PLATFORM_ARCHITECTURE_MAP.md)**). Delivery is deterministic-first and phased. **Phase 2.0.1 — Automation Foundation, Job Execution & Audit** (the spine: organization-scoped job execution, an explicit Automation Principal, an immutable `AutomationExecution` ledger separated from a mutable `AutomationJob` lifecycle, compound-unique idempotency, classified-failure retry with exponential backoff + dead-letter, a mandatory pure policy gate, and crash recovery via a lease + reaper — all proven by a single harmless internal **read-only** proof job that observes the existing closing projection and produces **no** domain effect) has a complete **repository-grounded planning package** ([Implementation Plan](../architecture/VERSION_2_0_PHASE_2_0_1_IMPLEMENTATION_PLAN.md) · [Schema Proposal](../architecture/VERSION_2_0_PHASE_2_0_1_SCHEMA_PROPOSAL.md) · [Acceptance Criteria](../architecture/VERSION_2_0_PHASE_2_0_1_ACCEPTANCE_CRITERIA.md) · [Test Plan](../architecture/VERSION_2_0_PHASE_2_0_1_TEST_PLAN.md) · [Rollout Plan](../architecture/VERSION_2_0_PHASE_2_0_1_ROLLOUT_PLAN.md) · [ADRs 0001–0005](../architecture/adr/)) grounded in the `RefreshJob`, email-outbox, `ActivityLog`, and RBAC precedents. It is **🟡 FOUNDER APPROVED FOR IMPLEMENTATION** — no 2.0 code, schema, migration, feature branch, or production change exists, and the one material piece of net-new infrastructure it proposes (a dedicated `crowdexpanse-automation` PM2 process — the platform's first out-of-request executor) is called out explicitly. Frozen V1.3 (`v1.3.0`) and V1.4 (`v1.4.0`) remain untouched.

### Security Model
Session-cookie auth → middleware gate → per-request `requireUser`/`requireRole` → org-scoped queries. `getCurrentUser` re-reads the user each request, so only `ACTIVE` accounts hold a live session; deactivation sets a per-user `sessionsValidAfter` epoch that invalidates every previously-issued cookie at once (stateless signed cookies, no server-side store). Uploads are size-capped and path-guarded. Secrets via env (`SESSION_SECRET`, `DATABASE_URL`). RBAC is enforced through the Authorization Principles below. **Redirects: `lib/safe-redirect.ts` is the canonical validator for any user-supplied redirect destination** (form `redirectTo`, `next` params, etc.) — it accepts internal relative paths only and rejects protocol-relative/absolute/backslash/control-char inputs. Server actions that redirect to a client-provided target MUST route it through `safeInternalPath()` rather than trusting it or hand-rolling a check (introduced with Owner linking, Commit 1d-2a). Future: audit hardening, rate limiting, and **password reset (Slice 3e, now scheduled in [1.2](./VERSION_1_2.md))** — see [Tech Debt](./TECHNICAL_DEBT.md).

### Authorization Principles
The permission model has one policy source (`lib/permissions.ts`, pure) and one enforcement/audit layer (`lib/authorize.ts`). Every contributor must follow these rules; the [Permissions module roadmap](./MODULE_ROADMAPS.md#permissions) links here.

1. **UI is never authoritative.** Hidden buttons and route guards are convenience and defense-in-depth only. The server is the sole source of truth.
2. **Server actions always enforce permissions.** Every write-bearing server action checks the policy before mutating — never rely on the caller having been gated upstream.
3. **`authorization.denied` is logged only for attempted mutations.** A denied *action* is a security event and is recorded (actor, role, resource, action, target, timestamp). Page loads, hidden-control checks, and route-guard `notFound()`s are **not** logged — use the pure `can()`/`canMoveStage()` for those, and `authorize()`/`checkAuthorized()`/`authorizeStageMove()` (which log) only inside server actions.
4. **Every new write action must call `authorize()`** (or `checkAuthorized()` for state-returning actions; the stage-move variants for pipeline changes). No write reaches Prisma without passing the policy.
5. **Every new feature must declare its resource/action mapping** in `lib/permissions.ts` (the `Resource`/`Action` matrix, and `canMoveStage` for any staged workflow) — the matrix is the single place the model is defined.

### Organization Scoping
The load-bearing invariant: **no query crosses `organizationId`.** Every new model, query, and E2E must assert org isolation (the E2E suite already does per module).

### AI Boundaries
AI is **out of scope until Volume 6 / v2.0.** When introduced: AI never writes to the DB without human confirmation; every AI output has a deterministic fallback and a human override; AI is scoped to a single org's data. See [AI_ROADMAP.md](./AI_ROADMAP.md). The Version 2.0 automation/AI boundaries are now formally locked in the **[Automation Architecture Lock](../architecture/AUTOMATION_ARCHITECTURE_LOCK.md)** (FOUNDER RATIFIED 2026-07-16; AI advisory, five-version-stamped, schema-validated, walled off from calculations, never authoritative — AU-5/AU-9); Phase 2.0.1 (the spine) contains **no AI** and is deterministic-only.

### Deployment Architecture
Single VPS, pm2-managed Next server on :3030, Caddy TLS/reverse-proxy, local Postgres 16, local document volume. CI builds on GitHub-hosted runners against an ephemeral Postgres. Future: managed DB, object storage, staging environment — see [Operations](./OPERATIONS_ROADMAP.md).

### Testing Architecture
See [Volume 8](./TESTING_ROADMAP.md). Today: deterministic E2E integration scripts against a dedicated test DB behind a `_test`-only guard, plus a pure-logic unit layer (`node:test`+`tsx`, `tests/unit/**`) with an enforced branch-coverage gate — line coverage is unreliable under `tsx` so branch% is the gate (revisit on a Node 22 upgrade, [D11](./TECHNICAL_DEBT.md)). Run locally (`npm test`, `npm run test:unit`) and in CI (`test:ci` + build).

---

## Development Lifecycle

> **How-to companion:** the [Engineering Playbook](../architecture/ENGINEERING_PLAYBOOK.md) is the living standard for *how* we execute this lifecycle — coding standards, testing patterns, the review checklist, anti-patterns, and the engineering decision framework. Each completed slice also gets a permanent retrospective under this directory (first: [Slice 1](./SLICE_1_RETROSPECTIVE.md)).

Every feature — human- or AI-authored — follows this, in order. **Nothing skips a step.**

```
Roadmap → Architecture → Specification → Implementation → Testing → Documentation → Merge → Release
```

1. **Roadmap** — the work exists in a version file and its Module Roadmap.
2. **Architecture** — confirm it fits Volume 2 (scoping, security, boundaries); note deviations.
3. **Specification** — a short written slice: scope, non-goals, data changes, acceptance criteria.
4. **Implementation** — smallest coherent slice; matches surrounding code idiom.
5. **Testing** — E2E (and unit where logic warrants) proving the acceptance criteria; org isolation asserted.
6. **Documentation** — update the Module Roadmap, Dashboard %, and any new Tech Debt.
7. **Merge** — feature branch → verify (typecheck, `npm test`, isolated build) → fast-forward to `main` → push both remotes.
8. **Release** — grouped into a version per [RELEASE_PLAN.md](./RELEASE_PLAN.md); tag when the version's checklist passes.

## Definition of Done

A change is **Done** only when all hold:
- [ ] Scoped to a slice with written acceptance criteria.
- [ ] Org-scoped; no cross-tenant leakage; roles enforced where relevant.
- [ ] `npm run typecheck` clean.
- [ ] Focused E2E added/updated and `npm test` green on the `_test` DB.
- [ ] Build passes (`npm run build` in CI; `build:isolated` locally).
- [ ] No unrelated files; no schema drift unless intended and reviewed.
- [ ] Module Roadmap + Executive Dashboard updated; new debt logged.
- [ ] Merged to `main` and pushed to Gitea + GitHub mirror.

## Governance
- The EMP is reviewed at the start of every release.
- Any architectural decision that contradicts Volume 2 requires an explicit note here (an ADR entry) before merge.
- The Executive Dashboard is the single source of truth for "what state is each module in."
